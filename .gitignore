/node_modules
/.next
/out
/build
next-env.d.ts
*.tsbuildinfo
.env
.env.local
.env*.local
!.env.example
.DS_Store
.vercel
.gstack/
# local app state (token hashes, github token, repo clones) — never commit
.engram/
**/.engram/
.engram-data/
sample-vault/.gitignore
# local working notes (not part of the OSS repo)
HANDOFF.md

# local design/roadmap notes — never published
.notes/
